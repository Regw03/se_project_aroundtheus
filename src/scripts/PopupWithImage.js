import Popup from "./scripts/Popup.js";

class PopupWithImage extends Popup{
    // constructor(popupSelector){
    //     super(popupSelector);

    //     this.imagePreviewTitle = this._popupElement.querySelector(".popup__image-title");
    // }

    openModel({link, name}) {
        
    }
}